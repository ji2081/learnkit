# learnkit 코어(콘솔 렌더·에러튜터 규칙기반)는 추가 설치 없이 동작합니다.
# 아래는 각 기능을 켤 때 필요한 선택 의존성:
rich          # 콘솔 예쁜 출력
ipywidgets    # 주피터 위젯 렌더러
streamlit     # 웹앱 렌더러 실행
anthropic     # (선택) LLM 에러 설명 — 없으면 규칙 기반 폴백
pyttsx3       # (선택) 읽어주기(TTS) — 없으면 화면에 텍스트로 대체
