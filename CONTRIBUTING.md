# 기여 안내

learnkit은 다양한 학습자(특수학급부터 대학까지)를 위한 파이썬 학습 도구를
함께 만들어가는 프로젝트입니다. 작은 기여도 환영합니다.

## 개발 환경

```bash
git clone https://github.com/ji2081/learnkit.git
cd learnkit
pip install -e ".[all]" pytest
python -m pytest          # 테스트 실행
```

## 가장 쉬운 입구 세 가지

**1. 에러 설명 한 줄 추가** — 초보자가 자주 만나는데 아직 설명이 없는 에러가 많습니다.

```python
from learnkit import register_rule

register_rule("JSONDecodeError",
              "JSON 모양이 아니에요.",
              "따옴표와 쉼표를 확인해 보세요.")
```

영구 반영은 `learnkit/error_tutor.py`의 `_RULES`에 한 줄 추가하면 됩니다.

**2. 새 화면(렌더러) 붙이기** — 상속도 등록 절차도 없습니다. 모양만 맞으면 됩니다.

```python
from learnkit import register

@register
class 내렌더러:
    name = "mine"

    def render(self, view, **options):
        ...
```

`demo_renderer.py`에 마크다운 렌더러 예제가 있습니다.

**3. 수업 자료 만들어보고 안 되는 점 알려주기** — 실제로 써봐야 보이는 게 제일 많습니다.

## 지금 열려 있는 과제

- [ ] **스크린리더 지원** — 위젯/웹앱 렌더러에 ARIA 라벨, 키보드 내비게이션 (가장 필요한 부분)
- [ ] **한 줄씩 따라가기 개선** — 지금은 지목한 함수의 프레임만 따라갑니다.
      중첩 함수·재귀·리스트 컴프리헨션(3.11 이하)은 안쪽이 안 보입니다
- [ ] **에러 설명 확충** — 현재 19종. 학교 현장에서 자주 나오는 것들을 더
- [ ] **막대 말고 다른 시각화** — 지금은 `█` 하나뿐입니다
- [ ] **위젯 렌더러 테스트** — `ipywidgets`가 필요해서 아직 자동 테스트가 없습니다
- [ ] **한글 정규화** — NFD로 조합된 한글은 폭 계산이 어긋납니다 (`disp_width`)

## 원칙

- **코어는 추가 설치 없이 동작해야 합니다.** rich·streamlit이 없어도 콘솔 렌더와 에러 튜터는 돌아가야 해요. 학교 컴퓨터에는 아무것도 못 깔 때가 많습니다.
- 무거운 의존성은 `[project.optional-dependencies]`로.
- **Lesson은 화면을 몰라야 합니다.** 렌더러가 Lesson 내부에 손대기 시작하면 "한 번 정의하면 어디로든"이 깨집니다. 필요한 게 있으면 `View`에 추가해 주세요.
- 학습자에게 보이는 문구는 쉬운 한국어로. 초등학생이 읽어도 이해되게.
- 한국어 주석 환영합니다.

## 테스트

기능을 추가하면 테스트도 같이 부탁드립니다. 특히 **학습자가 이상한 값을 넣었을 때**를
많이 봐주세요 — 음수, 소수, 빈 값, 아주 큰 수. 수업 중에 실제로 벌어지는 일들입니다.
