# 변경 기록

이 파일은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/) 형식을 따릅니다.

## [미공개]

### 고침

- **LLM 전송을 기본값 꺼짐으로 전환.** 학습자가 쓴 코드가 동의 없이 외부 API로
  나가고 있었습니다. 이제 `use_llm=True`로 명시해야 켜집니다.
- **접근성 기본값 유지.** 큰 글씨와 색 대비가 `보기` 단계에서만 켜져 있어,
  `바꾸기`로 넘어가면 꺼졌습니다. 세 단계 모두 켜진 채로 둡니다.
- **무한 루프에서 돌아오지 않던 문제.** `max_steps`에 닿으면 기록만 멈추고 실행은
  계속했습니다. 이제 실행 자체를 끊고 "멈췄습니다" 한 줄을 남깁니다.
- **`trace=True`일 때 `build`가 두 번 실행되던 문제.** 파일을 쓰거나 카운터를
  올리는 `build`에서는 그 일이 두 번 일어났습니다.
- **렌더러 입력 방어.** 자릿수가 매우 큰 숫자에서 `bar()`가 터지던 것,
  학습자 데이터가 HTML로 해석되던 것, 값에 `|`가 들어가면 마크다운 표가 깨지던 것,
  폭이 좁을 때 `blocks`의 자르기 루프가 끝나지 않던 것.
- **AI 호출 타임아웃 8초.** SDK 기본값이 10분이라 네트워크가 막히면 수업이 멈췄습니다.

### 성능

- **값 요약을 `reprlib`으로 교체.** 30만 원소 리스트 추적이 7.65초에서 0.02초가
  됐습니다. `repr()`로 전체 문자열을 만든 뒤 60자만 남기고 버리고 있었습니다.

### 문서

- "Did you mean" 도입 버전을 3.12에서 **3.10**으로 정정했습니다.
  확인하다 보니 더 중요한 사실이 있었습니다 — `sys.excepthook`을 가로채면
  그 제안이 사라집니다. 우리가 하는 일이 바로 가로채는 것입니다.
- `pip install learnkit` 안내를 뺐습니다. PyPI에 이름이 같은 다른 패키지가 있습니다.
- README에 실제 동작 화면을 넣었습니다.

### 그 밖에

- 대표 예제를 리스트 컴프리헨션에서 `for` 문으로 바꿨습니다. 컴프리헨션은 파이썬
  3.11 이하에서 별도 코드 객체라 "한 줄씩 따라가기"에 안쪽이 잡히지 않습니다.
- 회귀 테스트 9개 → 83개.

## [0.1.0] — 2026-08-07

첫 공개. 흥미가 꺼지는 네 지점에 붙인 네 가지 장치.

- `sys.settrace` — 실행 중간을 한 줄씩 연다
- `inspect.getsource` — 방금 실행된 함수를 그대로 보여준다
- `typing.Protocol` — 상속 없이 새 화면을 추가한다
- `sys.excepthook` · `difflib` — 에러를 한국어로, 오타를 짚어준다
